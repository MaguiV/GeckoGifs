import React from 'react';
import ReactDOM from 'react-dom/client';
import { GeckoGifs } from './GeckoGifs';
import './styles.css';


ReactDOM.createRoot(document.getElementById('root')).render(
     <React.StrictMode>
      <GeckoGifs />
    </React.StrictMode>
);
